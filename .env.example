# =============================================================================
# Nutrient Viewer POC - Environment Configuration
# =============================================================================
# Copy this file to .env and configure your values
# See README.md for key generation instructions

# -----------------------------------------------------------------------------
# Document Engine Configuration
# -----------------------------------------------------------------------------

# URL where Document Engine is running
DE_URL=http://localhost:5000

# API token for server-to-server authentication
DE_API_AUTH_TOKEN=secret

# Secret key base for encryption (change in production)
DE_SECRET_KEY_BASE=secret-key-base-change-in-production

# Dashboard credentials
DE_DASHBOARD_USERNAME=dashboard
DE_DASHBOARD_PASSWORD=secret

# License activation key (leave empty for trial mode)
DE_ACTIVATION_KEY=

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
# Generate keys with: openssl genrsa -out private.pem 2048
#                     openssl rsa -in private.pem -pubout -out public.pem

# Private key for JWT signing (used by server)
# Paste the contents of private.pem here (with \n for newlines)
DE_JWT_PRIVATE_KEY=

# Public key for JWT verification (used by Document Engine)
# Paste the contents of public.pem here (with \n for newlines)
DE_JWT_PUBLIC_KEY=

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port for the h3 API server
SERVER_PORT=3001

# -----------------------------------------------------------------------------
# Web App Configuration (Vite)
# -----------------------------------------------------------------------------

# URL of the Document Engine (for client-side connections)
VITE_DE_URL=http://localhost:5000
